# Server-side only. Never prefix this variable with PUBLIC_ or expose it in browser code.
ANTHROPIC_API_KEY=replace-with-your-server-side-key

# Optional model override.
CLINICSCOUT_MODEL=claude-haiku-4-5-20251001
